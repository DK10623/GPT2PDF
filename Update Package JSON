{
	"name": "ChatGPT-to-PDF",
	"version": "1.0.0",
	"description": "A Chrome plugin that converts ChatGPT conversations to PDFs with code snippets formatted for GitHub and tables formatted for CSVs.",
	"manifest_version": 2,
	"permissions": ["activeTab"],
	"content_scripts": [
		{
			"matches": ["<all_urls>"],
			"js": ["index.js"]
		}
	]
}
